-- Math Auto Solver (para usar con HttpGet en un executor) -- NOTA: Debes adaptar getQuestion() y sendAnswer() a cómo el juego presenta la pregunta y acepta la respuesta. -- Usa la URL raw de este archivo en GitHub para cargarlo con HttpGet en tu executor. local POLL_INTERVAL = 0.35 -- segundos -- Sanitiza la expresión: permite sólo dígitos, espacios y operadores básicos local function sanitize(expr) if not expr then return nil end expr = tostring(expr) -- Permitimos: 0-9, + - * / ^ % . ( ) y espacios if expr:match("[^%d%+%-%*%/%^%%%.%(% )]") then return nil end return expr end -- Intenta evaluar la expresión de forma segura usando loadstring (común en executors) local function safeEval(expr) local s = sanitize(expr) if not s then return nil, "expression contains invalid chars" end local f, err = loadstring("return " .. s) if not f then return nil, "loadstring error: " .. tostring(err) end local ok, res = pcall(f) if not ok then return nil, "eval error: " .. tostring(res) end return res end -- Ejemplo: función que obtiene la pregunta del juego. -- Debes reemplazar el cuerpo por la forma en que "Math Answer or Die" expone la pregunta. local function getQuestion() -- Ejemplo ficticio (NO funcionará tal cual): -- local playerGui = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui") -- local screen = playerGui:FindFirstChild("ScreenGui") -- local label = screen and screen:FindFirstChild("MathQuestion") -- if label then return label.Text end return nil -- <-- REEMPLAZA con la lógica real end -- Ejemplo: función que envía la respuesta al juego. -- Reemplaza por la llamada adecuada: RemoteEvent:FireServer(answer) o cambiar TextBox y presionar botón, etc. local function sendAnswer(answer) -- Ejemplo ficticio: -- local rep = game:GetService("ReplicatedStorage") -- local submit = rep:FindFirstChild("SubmitAnswer") -- if submit and submit:IsA("RemoteEvent") then -- submit:FireServer(tostring(answer)) -- end return false -- <-- REEMPLAZA con la lógica real; devuelve true si enviaste la respuesta end -- Extrae una expresión matemática simple del texto de la pregunta. local function extractExpression(text) if not text then return nil end local match = text:match("([%d%+%-%*%/%^%%%.%(% )]+)") if match then match = match:match("^%s*(.-)%s*$") return match end return nil end -- Loop principal local lastQuestion = nil while true do local questionText = getQuestion() if questionText and questionText ~= lastQuestion then lastQuestion = questionText local expr = extractExpression(questionText) if expr then local result, err = safeEval(expr) if result ~= nil then local formatted = tostring(result) local sent = sendAnswer(formatted) if not sent then print("[MathAutoSolver] Resuelto:", expr, "=", formatted) end else warn("[MathAutoSolver] No pude evaluar:", expr, "->", err) end else warn("[MathAutoSolver] No pude extraer expresión de:", questionText) end end wait(POLL_INTERVAL) end
